export default {
  // This property defines plugins to be used during the build process
  plugins: {
    // Tailwind CSS plugin configuration (left empty for default behavior)
    tailwindcss: {},
    // Autoprefixer plugin configuration (left empty for default behavior)
    autoprefixer: {},
  },
};
